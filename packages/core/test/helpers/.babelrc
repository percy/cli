{
  "plugins": [
    ["module-resolver", {
      "alias": {
        // for coverage during tests without needing to compile helper
        "@percy/core/dist/server": "./src/server"
      }
    }]
  ]
}
