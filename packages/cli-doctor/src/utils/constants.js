// Known token prefixes → project type names.
// Canonical source: @percy/client tokenType() — keep in sync.
export const KNOWN_PREFIXES = {
  auto: 'automate',
  web: 'web',
  app: 'app',
  ss: 'generic',
  vmw: 'visual_scanner',
  res: 'responsive_scanner'
};
