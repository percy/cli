const { Create } = require('./commands/config/create');
const { Migrate } = require('./commands/config/migrate');
const { Validate } = require('./commands/config/validate');

module.exports = { Create, Migrate, Validate };
